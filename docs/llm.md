# llm — apps/server/src/llm (the only AI-SDK site)

Purpose: normalize provider streaming/usage quirks behind one owned seam (`LlmClient`) while the ContextAssembler keeps 100% ownership of prompt content and order (FINAL item 9). `ai` + `@openrouter/ai-sdk-provider` are import-fenced here (A11).

## Contract

- Inputs: `LlmCall` — `{ kind, characterId, system: stablePrefix, prompt: dynamicTail, onTextDelta }`.
- Outputs: `Result<LlmCallResult>` — full text + `{inputTokens, outputTokens, cachedInputTokens}` + model + duration. Provider failures are `err(operational)`, never throws (C2).
- Never: reorder or rewrite prompt content; let SDK exceptions escape; log prompt content above `trace` (C12 — the debug line carries token counts only).

## File table

| File | What it does / talks to |
| --- | --- |
| `types.ts` | The seam: `LlmClient`, `LlmCall`, `LlmUsage`. Everything outside this dir imports only this. `CallKind` covers the scripted turn (`narrator`/`character`/`narration`) plus the cold-path kinds `reflection`, `world_agent`, `materialize` (M4 part 2: the sublocation-stub generation), `map_edit` (M5 part 2: the Flow-A GM form) and `jump_in` (M5 part 2: the Flow-B story invention inside a VLM classification). M3: `LlmCall.toolset` ('narrator') offers tools; `LlmCallResult.toolCalls` returns RAW calls — callers must run both B6 gates. M6 part 1: `LlmCall.queries` — engine-owned READ-ONLY query executors the client runs mid-call (multi-step), feeding the result string back to the model; queries route context (Rev 4 §6), mutations always come back as data. |
| `tools.ts` | Narrator tool definitions (`end_scene` — now with the optional `next_scene` continuation registration, `change_sublocation`, `switch_art`, `create_sublocation` — M6 part 1: name/brief/parent_id?/narrative_anchor?, `query_sublocations` — three modes: parentless \| children \| search) + `parseToolCall` — gate 1 of the B6 double gate (our own safeParse via `validateAt('llm', …)`, even when the provider "guaranteed" the shape). `query_sublocations` is never stageable (it executes mid-call); one arriving as data is rejected as a value. Gate 2 lives in `engine/scene-tools.ts`. |
| `model-registry.ts` | character/function → model+provider order+params; per-character pinning keeps prompt caches warm (owner decision #3). Config from env (`WELTARI_MODEL`, `WELTARI_PROVIDER_ORDER`). |
| `openrouter-client.ts` | `streamText` against OpenRouter; provider pinning via `extraBody.provider.order` (`allow_fallbacks:false`); usage accounting on (`usage.include`; `totalUsage` sums every step); `cached_tokens` extracted defensively from SDK usage or OpenRouter metadata; per-call `debug` log = the cache-hit observability (risk register #1). 120 s abort. M3: passes the narrator toolset as SDK `tool()`s with Zod inputSchemas (mutating tools carry no execute — calls come back as data for the gates). M6 part 1: when the call offers `queries`, `query_sublocations` gets an execute (the engine's read-only executor) + `stopWhen: stepCountIs(3)` — query → result → the model keeps generating in ONE call; mutating tool calls are collected across ALL steps, executed queries filtered out. |
| `structured.ts` | `parseLlmJson` — the ONE audited JSON.parse site for model output (B-llm): raw text or first ```json fence -> `unknown` for `validateAt`; null on failure (reject, never repair — B4). |
| `image-source.ts` | `createOpenRouterImageSource` (M5 part 1) — the painter's real tile backend, living here because `ai` + the provider are fenced in llm/ (A11). `generateImage` against OpenRouter's `/v1/images` endpoint (`WELTARI_IMAGE_MODEL`); with a context window it runs in EDIT mode — the window travels as an `input_references` image and the model continues the existing painting into the checkerboard fog (cross-tile geometric coherence; the checkerboard is the region-in-words fog marker, Rev 4 §14 no-mask branch), returning coverage 'window' for the compositor to cut the target from. M5 part 2: `mode 'modify'` (Flow-A edits) switches to the REPLACE framing (the window carries the painter's red outline marking the drawn area) and routes to `WELTARI_EDIT_IMAGE_MODEL` (default `google/gemini-3-pro-image`, ~4× — week-8 visual QA: flash-class never painted the drawn feature legibly; edits are rare and user-triggered). Aspect ratio follows the window (1:1 / 2:3 / 3:2 at edges); the compositor resizes (Rev 4 §14 size rules). Provider failure → OperationalError → runner retry (C7); per-call `debug` log carries sizes/tokens + edit_mode only (C12). Selected in main.ts by `WELTARI_IMAGE_BACKEND=openrouter` + key; the painter's stub stays the default ([painter.md](painter.md)). |
| `vlm.ts` | The VLM seam (M5 part 1, B-llm): `VlmClient.describe({kind, prompt, image, mediaType}) → Result<{text, usage, model, durationMs}>` — image + prompt in, RAW text out; callers run gate 1 themselves (`parseLlmJson` → `validateAt`) and gate 2 before anything durable (B6). `kind` names the consumer (`map_qa` = the QA tool; `classify_click` = the M5 part 2 Flow-B map_click job). Cold-path `generateText`, temperature 0.2, `WELTARI_VLM_MODEL` (default `google/gemini-3.5-flash`); provider failure → `err(operational)` (C2). Consumers: `tools/m5-map-qa.mjs` ([tools.md](tools.md)) and `ledger/handlers/map-click.ts` ([ledger.md](ledger.md)). Tested offline through the provider's fetch seam. main.ts selects the real client only when the text LLM is real (not fake, key present); otherwise the fake VLM below. |
| `fake-client.ts` | Deterministic scripted double, selected by `WELTARI_FAKE_LLM=1`. Lives in src because the kill harness runs the real binary against it (I4). M3: scripts tool calls from public-API text triggers — `!end [type]`, `!move <id>`, `!art <char> <art>`, `!badshape`, `!ghosttool` — so tests, the harness and a browser can drive the whole B6 pipeline. M4 part 2: a deterministic JSON stub for `materialize` calls (malformed-stub rejection is driven by a stub client at the seam in the invariant tests). `WELTARI_FAKE_LLM_DELAY_MS` holds before each call's first token — the simulated 5–10 s generation window the §1.14 masking animations are verified against. M5 part 2: scripted `map_edit`/`jump_in` JSON forms + `createFakeVlmClient` (a fixed, schema-valid Flow-B classification; the image is ignored) — the harness and the fake browser demo classify clicks at $0.00. M6 part 1: `!create <name-slug> <parent_id>` (interior), `!createwild <name-slug>` (parentless — the query-first gate-2 subject), `!query` (runs the engine's query executor mid-call, mode parentless), `!endnext <sublocation_id>` (continuation + next_scene); creates come back before moves/ends so a scripted reply can create → move → end like a real narrator. |

## engine additions (scene turn)

| File | What it does / talks to |
| --- | --- |
| `engine/scene-turn.ts` | The scripted turn: `turn.started` durable first → Narrator (with the narrator toolset + the M6 query executor, dev-trailed per query) → character → narration (sequential, each streamed sentence-by-sentence to StreamBus) → ONE transaction committing `turn.committed` + staged tool events (+ scene.ended with fan-out when end_scene staged), published after commit. M6 part 1: a staged create appends `sublocation.stub_created` AND enqueues its backdrop paint (`painter:backdrop:<id>:initial`) in the same transaction; a parentless create also enqueues `materialize:stub:<id>` anchored at the scene's current sublocation; `sublocation.changed` carries `backdrop_path` when the backdrop already landed; `kickRunner` drains the ledger right after commit so the backdrop starts instantly. Any failure voids the turn — zero partial durable rows (B6). `interruptTurn` closes the envelope immediately at the user's last-seen sentence: truncated `turn.committed` (marked `interrupted`), staged tool effects discarded (stubs and their jobs included), later LLM output finishes into the void. Fault-point hooks `mid_stream`/`between_calls`/`pre_commit` for the kill harness. |
| `engine/sentences.ts` | Incremental sentence splitter (deltas in → whole sentences out). |

## Events consumed/emitted

Emits `turn.started`, `turn.committed` (actor = commanding actor). Reads `turn.committed` for the transcript tail.

## Tests

Turn ordering + stream indexes + fault sequence; mid-turn failure leaves only `turn.started`; stable prefix byte-identical across turns while the tail changes (the cache contract); sentence splitter; registry routing/pinning.
