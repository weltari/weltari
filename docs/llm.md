# llm — apps/server/src/llm (the only AI-SDK site)

Purpose: normalize provider streaming/usage quirks behind one owned seam (`LlmClient`) while the ContextAssembler keeps 100% ownership of prompt content and order (FINAL item 9). `ai` + `@openrouter/ai-sdk-provider` are import-fenced here (A11).

## Contract

- Inputs: `LlmCall` — `{ kind, characterId, system: stablePrefix, prompt: dynamicTail, onTextDelta }`.
- Outputs: `Result<LlmCallResult>` — full text + `{inputTokens, outputTokens, cachedInputTokens}` + model + duration. Provider failures are `err(operational)`, never throws (C2).
- Never: reorder or rewrite prompt content; let SDK exceptions escape; log prompt content above `trace` (C12 — the debug line carries token counts only).

## File table

| File | What it does / talks to |
| --- | --- |
| `types.ts` | The seam: `LlmClient`, `LlmCall`, `LlmUsage`. Everything outside this dir imports only this. `CallKind` covers the scripted turn (`narrator`/`character`/`narration`) plus the cold-path kinds `reflection`, `world_agent`, `materialize` (M4 part 2: the sublocation-stub generation), `map_edit` (M5 part 2: the Flow-A GM form) and `jump_in` (M5 part 2: the Flow-B story invention inside a VLM classification). M3: `LlmCall.toolset` ('narrator') offers tools; `LlmCallResult.toolCalls` returns RAW calls — callers must run both B6 gates. M6 part 1: `LlmCall.queries` — engine-owned READ-ONLY query executors the client runs mid-call (multi-step), feeding the result string back to the model; queries route context (Rev 4 §6), mutations always come back as data. M6 part 2: `LlmCall.gate` — the engine's B6 double gate offered as a mid-call executor: mutating tool calls gate DURING the call and the model reads the staged-ack or the refusal (tool ERROR) and can self-correct in the same turn; staging stays in-memory, durability still only at turn.committed; gated calls are never returned in `toolCalls`. |
| `tools.ts` | Narrator + chat tool definitions (`end_scene` — now with the optional `next_scene` continuation registration, `change_sublocation`, `switch_art`, `create_sublocation` — M6 part 1: name/brief/parent_id?/narrative_anchor?, `query_sublocations` — three modes: parentless \| children \| search) + `parseToolCall` — gate 1 of the B6 double gate (our own safeParse via `validateAt('llm', …)`, even when the provider "guaranteed" the shape). `query_sublocations` is never stageable (it executes mid-call); one arriving as data is rejected as a value. Gate 2 lives in `engine/scene-tools.ts`. M6 part 2: the CHAT toolset (`cache` — the mandatory per-reply recap line, Rev 4 §11; `startscene` — the chat-side bridge, Rev 4 §8: required `place` = existing sublocation or free text, characters can never create) + `parseChatToolCall` (gate 1 for chat replies); chat tools are data-only, gated in `engine/chat.ts`. M6 part 3: the `startscene` description teaches the character-led firing rule (fire it YOURSELF once the place is agreed, never before — the User cannot open it); the negotiation itself lives in `CHAT_CONDUCT_SKILL` ([engine.md](engine.md)); the chat query escalation (Rev 4 §11) adds `WikiqueryToolSchema`/`SessionqueryToolSchema` + `CHAT_QUERY_DESCRIPTIONS` — mid-call-only like `query_sublocations` (one arriving as data is rejected as a value); M7 part 1 adds `MemoryqueryToolSchema` to the same family (the FTS5 deep dive into the character's own deltas — chat AND character scene turns, owner ruling 2026-07-11). M6 part 4 (0.13.0, Rev 4 §7, owner ruling 2026-07-10/11): `startscene` gains REQUIRED `wait_hours` — the character's own GAME-time window ("how many in-world hours you will wait before giving up"), its model decision, never an env default; the description and `CHAT_CONDUCT_SKILL` teach it neutrally, and a call without it fails gate 1 into the chat engine's correction loop (ceiling 10 → rollback + `chat.notice`); `stay_silent` — the character's explicit decline of a proactive fire (owner ruling 2026-07-11: never an empty reply; data-only, the handler leaves the fire quiet). M6 part 5 (Rev 4 §12): the SOCIAL_REACT toolset (`react` — kind like \| comment with a one-line body; plus stay_silent and cache) + `parseSocialToolCall` (gate 1 for reaction decisions); the react description teaches the isolation rule (comments never thread between characters). M7 part 1 (Rev 4 §11, owner rulings 2026-07-11): the REFLECTION toolset — `memory_delta` (one lasting note, 1–3 calls), `update_core` (the FULL replacement core snapshot, ≤12 lines), `evolve` (full personality/goals replacements — the description says rare and earned) — + `parseReflectionToolCall` (gate 1); all data-only, gate 2 is `engine/memory.ts gateReflectionMemory` (delta cap, last-core-wins, the locked refusal, prefix-hygiene sanitization). M7 part 2 (Rev 4 §9/§16): the GM toolset — `propose_place` / `propose_character` / `propose_wiki_edit` / `propose_world_seed` (+ rationale each; the diff shapes mirror the wire's ProposalPlaceDiff/ProposalCharacterDiff on purpose — drift is refused visibly at the submit seam) + `parseGmToolCall` (gate 1); ALL data-only proposals — gate 2 and the payload re-shape live in `engine/proposals.ts`, and nothing the GM says is durable until the user approves the card; the descriptions teach the consent contract ("nothing is created yet", "do not call the seed while anything essential is unasked"). |
| `model-registry.ts` | character/function → model+provider order+params; per-character pinning keeps prompt caches warm (owner decision #3). Config from env (`WELTARI_MODEL`, `WELTARI_PROVIDER_ORDER`). M7 part 2: the GM's per-role override rides the per-character key `char:gm` (`WELTARI_GM_MODEL`, absent = the default — Rev 4 §16 model routing without new machinery); `CallKind` gains `gm`. |
| `openrouter-client.ts` | `streamText` against OpenRouter; provider pinning via `extraBody.provider.order` (`allow_fallbacks:false`); usage accounting on (`usage.include`; `totalUsage` sums every step); `cached_tokens` extracted defensively from SDK usage or OpenRouter metadata; per-call `debug` log = the cache-hit observability (risk register #1). 120 s abort. M3: passes the narrator toolset as SDK `tool()`s with Zod inputSchemas (mutating tools carry no execute — calls come back as data for the gates). M6 part 1: when the call offers `queries`, `query_sublocations` gets an execute (the engine's read-only executor) + `stopWhen: stepCountIs(3)` — query → result → the model keeps generating in ONE call; mutating tool calls are collected across ALL steps, executed queries filtered out. M6 part 2: when the call offers `gate`, the four mutating tools ALSO get an execute — the engine's double gate returning "staged: …" or "ERROR: …" as the tool result (step limit 4: refusal → query → corrected retry → closing text) and `toolCalls` comes back empty (everything already gated engine-side; returning them again would double-stage). M6 part 3: the CHAT toolset gains the same multi-step query wiring (`chatToolsFor`) — `wikiquery`/`sessionquery` execute mid-call when the call offers them (step limit 3); `LlmCall.queries` fields are all optional now. M6 part 4: toolset `group_router` — the Group-chat Narrator's data-only route/endsubsession pair (kind `group_route`); the chat toolset also offers stay_silent. M6 part 5: toolset `social_react` — the data-only reaction decision trio (react/stay_silent/cache); toolset `social_reply` — ANSWER-ONLY (cache alone: no tools = no promises to break, owner ruling 2026-07-11); kinds `social_post`/`social_react`/`social_reply` route like every other call. M7 part 1: toolset `reflection` (memory_delta/update_core/evolve, data-only); toolset `character_scene` — a character's scene turn carries ONLY the read-only queries (memoryquery/wikiquery, each with an execute, step limit 3); the chat toolset gains memoryquery through the shared `queryToolsFor`; executed queries of every name are filtered out of the returned `toolCalls`. |
| `structured.ts` | `parseLlmJson` — the ONE audited JSON.parse site for model output (B-llm): raw text or first ```json fence -> `unknown` for `validateAt`; null on failure (reject, never repair — B4). |
| `image-source.ts` | `createOpenRouterImageSource` (M5 part 1) — the painter's real tile backend, living here because `ai` + the provider are fenced in llm/ (A11). `generateImage` against OpenRouter's `/v1/images` endpoint (`WELTARI_IMAGE_MODEL`); with a context window it runs in EDIT mode — the window travels as an `input_references` image and the model continues the existing painting into the checkerboard fog (cross-tile geometric coherence; the checkerboard is the region-in-words fog marker, Rev 4 §14 no-mask branch), returning coverage 'window' for the compositor to cut the target from. M5 part 2: `mode 'modify'` (Flow-A edits) switches to the REPLACE framing (the window carries the painter's red outline marking the drawn area) and routes to `WELTARI_EDIT_IMAGE_MODEL` (default `google/gemini-3-pro-image`, ~4× — week-8 visual QA: flash-class never painted the drawn feature legibly; edits are rare and user-triggered). Aspect ratio follows the window (1:1 / 2:3 / 3:2 at edges); the compositor resizes (Rev 4 §14 size rules). Provider failure → OperationalError → runner retry (C7); per-call `debug` log carries sizes/tokens + edit_mode only (C12). Selected in main.ts by `WELTARI_IMAGE_BACKEND=openrouter` + key; the painter's stub stays the default ([painter.md](painter.md)). |
| `vlm.ts` | The VLM seam (M5 part 1, B-llm): `VlmClient.describe({kind, prompt, image, mediaType}) → Result<{text, usage, model, durationMs}>` — image + prompt in, RAW text out; callers run gate 1 themselves (`parseLlmJson` → `validateAt`) and gate 2 before anything durable (B6). `kind` names the consumer (`map_qa` = the QA tool; `classify_click` = the M5 part 2 Flow-B map_click job). Cold-path `generateText`, temperature 0.2, `WELTARI_VLM_MODEL` (default `google/gemini-3.5-flash`); provider failure → `err(operational)` (C2). Consumers: `tools/m5-map-qa.mjs` ([tools.md](tools.md)) and `ledger/handlers/map-click.ts` ([ledger.md](ledger.md)). Tested offline through the provider's fetch seam. main.ts selects the real client only when the text LLM is real (not fake, key present); otherwise the fake VLM below. |
| `fake-client.ts` | Deterministic scripted double, selected by `WELTARI_FAKE_LLM=1`. Lives in src because the kill harness runs the real binary against it (I4). M3: scripts tool calls from public-API text triggers — `!end [type]`, `!move <id>`, `!art <char> <art>`, `!badshape`, `!ghosttool` — so tests, the harness and a browser can drive the whole B6 pipeline. M4 part 2: a deterministic JSON stub for `materialize` calls (malformed-stub rejection is driven by a stub client at the seam in the invariant tests). `WELTARI_FAKE_LLM_DELAY_MS` holds before each call's first token — the simulated 5–10 s generation window the §1.14 masking animations are verified against. M5 part 2: scripted `map_edit`/`jump_in` JSON forms + `createFakeVlmClient` (a fixed, schema-valid Flow-B classification; the image is ignored) — the harness and the fake browser demo classify clicks at $0.00. M6 part 1: `!create <name-slug> <parent_id>` (interior), `!createwild <name-slug>` (parentless — the query-first gate-2 subject), `!query` (runs the engine's query executor mid-call, mode parentless), `!endnext <sublocation_id>` (continuation + next_scene); creates come back before moves/ends so a scripted reply can create → move → end like a real narrator. M6 part 2: scripted `chat` + `reflect_chat` replies; every chat reply carries the mandatory `cache` tool call, so tests/harness get real CACHE entries at $0.00; `!startscene <place-slug>` scripts the bridge (hyphens→spaces; wait_hours 6 since 0.13.0). M6 part 3: `!wikiquery <words>` / `!sessionquery <words>` run the chat query executors mid-call and the scripted reply VISIBLY embeds the result — the whole escalation drives at $0.00. M6 part 4: `!startscene-nowindow <place>` omits wait_hours until the `## Correction` round arrives (scripts retry-then-succeed); `!startscene-stubborn <place>` omits it every round (scripts ceiling exhaustion → the chat.notice rollback); `!staysilent` in the prompt scripts the explicit stay_silent decline (the proactive fire stays quiet at $0). M6 part 4 group-router scripts (read AFTER the last user line only, so markers never haunt later rounds): `!endsub` ends the round, `!route <char-id>` routes explicitly, default = the FIRST listed member — the engine budget is what stops the ping-pong. M6 part 5 feed scripts: kind `social_post` gets a scripted in-character post (its chat-branch cache line reads "Posted about the workshop roof…"); toolset `social_react` defaults to a one-line COMMENT + cache (the most demonstrable outcome) — `!like` scripts a bare like, `!staysilent` the decline, `!badreact` a gate-1 subject (comment without body); markers reach the decision through the post body riding the prompt. M7 part 1 memory scripts (toolset `reflection`; markers ride the scene/chat transcript into the reflection prompt): every scripted reflection commits deltas at $0 (two scene / one chat); `!memcore` adds an update_core snapshot, `!evolve` an evolve call (the gate-2 locked subject), `!overcap` five deltas (the cap subject), `!badmemory` a malformed delta (gate-1 subject), `!evolveempty` an empty evolve (gate-2 subject). `!memoryquery <words>` runs the FTS5 memory executor mid-call — in chat replies AND on the character's scene turn (toolset `character_scene`) — and the reply VISIBLY embeds the recalled delta (criterion c at $0); scene turns also honor `!wikiquery <words>`. M7 part 2 GM scripts (toolset `gm`, kind `gm`): `!proposeplace <name-slug>` (public place), `!proposeprivate <name-slug>` (private), `!proposechar <name-slug>`, `!proposewiki <sublocation_id>`, `!proposeseed <world-slug>` (the whole §9 cold-boot form: 3 places incl. the public+private mix + 2 characters), `!badproposal` (gate-1 subject); `!wikiquery <words>` runs the wiki executor mid-call for the GM too. |

## engine additions (scene turn)

| File | What it does / talks to |
| --- | --- |
| `engine/scene-turn.ts` | The scripted turn: `turn.started` durable first → Narrator (with the narrator toolset + the M6 query executor, dev-trailed per query) → character → narration (sequential, each streamed sentence-by-sentence to StreamBus) → ONE transaction committing `turn.committed` + staged tool events (+ scene.ended with fan-out when end_scene staged), published after commit. M6 part 1: a staged create appends `sublocation.stub_created` AND enqueues its backdrop paint (`painter:backdrop:<id>:initial`) in the same transaction; a parentless create also enqueues `materialize:stub:<id>` anchored at the scene's current sublocation; `sublocation.changed` carries `backdrop_path` when the backdrop already landed; `kickRunner` drains the ledger right after commit so the backdrop starts instantly. M6 part 2: the narrator call also offers `LlmCall.gate` — `gateOne` (parse + stage + trail frame, shared with the post-call loop) runs mid-call and the model reads `stagedAck(...)` or `ERROR: <reason>` as its tool result (the week-9 trail-only-rejection upgrade, owner decision 2026-07-09); an interrupted turn answers every later gate call with an ERROR and stages nothing. Any failure voids the turn — zero partial durable rows (B6). `interruptTurn` closes the envelope immediately at the user's last-seen sentence: truncated `turn.committed` (marked `interrupted`), staged tool effects discarded (stubs and their jobs included), later LLM output finishes into the void. Fault-point hooks `mid_stream`/`between_calls`/`pre_commit` for the kill harness. |
| `engine/sentences.ts` | Incremental sentence splitter (deltas in → whole sentences out). |

## Events consumed/emitted

Emits `turn.started`, `turn.committed` (actor = commanding actor). Reads `turn.committed` for the transcript tail.

## Tests

Turn ordering + stream indexes + fault sequence; mid-turn failure leaves only `turn.started`; stable prefix byte-identical across turns while the tail changes (the cache contract); sentence splitter; registry routing/pinning.
