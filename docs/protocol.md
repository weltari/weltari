# protocol — @weltari/protocol (MIT edge package)

Purpose: the language-neutral wire contract between the engine and every client (built-in web app, V1.5 CLI, future external games — Brief §1). Zod v4 schemas are the single source; committed JSON Schemas are emitted for non-JS clients (Guide §0.1, B9).

## Contract

- Inputs: none (leaf package).
- Outputs: Zod schemas + inferred types + `PROTOCOL_VERSION`; `schemas/*.json` (generated — never hand-edit, Guide §8.4).
- Never: import from `apps/*` (license fence A12); contain vendored third-party source; use `.parse()` (B1 applies here too).

## File table

| File | What it does / talks to |
| --- | --- |
| `src/index.ts` | Public surface: re-exports + `PROTOCOL_VERSION` (handshake semver; major bump required for breaking changes, I7). |
| `src/events.ts` | Durable event union (`scene.started`, `scene.ended` — now with optional `end_type`/`divider_text` for the soft-close button set, `character.joined` — the scene-roster projection (0.7.0: emitted per participant at scene open; clients render the VN line-up from these), `turn.started`, `turn.committed` — now with optional `interrupted`, `sublocation.changed`, `sublocation.materialized` — the fog projection (0.8.0: explored = materialized; carries the LLM stub's name/description, the fog-grid square and the pin's world coordinates; `MAP_FOG_GRID` = 8 is the shared grid contract), `art.switched`, `reflection.committed`, `world_agent.committed`, `world.time_advanced`, `world_cron.completed`, `painter.completed`, `update.available` + `update.staged` — the self-update path (B12: available = untrusted notice; staged = verified + pointer flipped, restart to apply), `map_edit.requested` + `sublocation.created` — Flow A (0.9.0: durable lasso-edit intent with the drawn polygon + capped intent text, then the GM-formed sublocation with pin at the mask centroid and the polygon as its footprint), `map_click.resolved` — Flow B (0.9.0: `created` = the persistent spawn's row, pin at the click point; `transient` = the display-once discovery, never a sublocation), `sublocation.stub_created` — the in-scene creation loop (0.10.0, Rev 4 §6: the Narrator's create_sublocation tool committed an identity stub atomically with its turn; `parent_id` present = an interior that never touches the map, absent = parentless, materialization enqueued eagerly; the backdrop painter job `backdrop:<sublocation_id>` fires in the same transaction), `scene.ended` optional `next_scene` (0.10.0: the continuation registration the "Jump to the next scene" button opens — may name a stub created this very turn), `job.failed`, `job.parked` — 0.9.0 adds optional `job_key` so clients tie failures back to their command (map-edit lock release), `chat.message_committed` + `chat.ended` + `reflect_chat.committed` + `cache.appended` — Weltari Chat part one (0.11.0, Rev 4 §8/§11: DM lines on the ONE event stream, message_id-idempotent; chat.ended closes a reflection range with reason exit|idle|startscene and names `range_end_id`; reflect_chat.committed is the ledger job's outcome, idempotent per (conversation, range_end_id); cache.appended is the CACHE store — a projection of these events, engine-written fields + the character's one-liner), `scene.started` optional `premise` + `place_request` (0.11.0: the chat→scene handoff surface — a free-text place the Narrator resolves via the standard create workflow on its first turn), `subwiki.updated` (0.11.0, Rev 4 §10: the World Agent's scene-end wiki entry per participating Narrator-created sublocation — observable-now snapshots with scene provenance; the wiki view is a projection), `chat.outreach_recorded` + `chat.thread_frozen` — proactive CRON DMs (0.12.0, Rev 4 §8: the fire's eager generation committed at fire time — the push IS the message; natural key world+occurrence_iso, stamped with the real fire time AND the fictional game_time (the V2 trigger-base bridge); thread_frozen = the 3-unanswered hard cap as a durable event — the future gateway push hook; a user reply resets the counter by construction), `scene.started` optional `invitation` + `scene.expired` + `chat.notice` — invitation expiry (0.13.0, Rev 4 §7, owner rulings 2026-07-10/11: the character chooses its GAME-time `wait_hours` as a required startscene parameter and the engine stamps `expires_at_game` at open; a never-entered invitation scene expires LAZILY when the user's own play moves the clock past the deadline — scene.expired closes the scene like scene.ended, releases presence, and rides one transaction with the hardcoded cache.appended absence entry, natural key scene_id; chat.notice is the durable red-line rollback notice after a critical tool chain exhausts its retry ceiling), `chat.group_started` + `chat.group_message_committed` + `chat.group_ended` — group chats (0.14.0, Rev 4 §8: user-started only, members fixed at start; lines unique per conversation+message_id; a range close carries member_ids and rides one transaction with exactly one reflect_chat job per member), `social.post_committed` + `social.reaction_committed` + `social.reply_posted` + `social.reply_answered` — the Feed (0.15.0, Rev 4 §12, M6 part 5: a post is the social CRON's eager generation at a GAME-time cadence boundary, natural key world+occurrence_iso, delivered by the acquaintance rule with recipient_ids recorded for audit; a reaction is ONE skill-triggered decision per picked recipient (like or one-line comment; stay_silent commits nothing), natural key post+character; the reply pair is the feed-local comment thread — the user replies (actor provenance) and the comment's author answers exactly once per user reply (natural key in_reply_to), never routed into Weltari Chat; `cache.appended` origin gains `social` so latest-per-origin keeps feed comments from shadowing scene memory), `subwiki.edited` — manual wiki edits (0.15.0, owner ruling 2026-07-11: the user's Wiki-page edit applies immediately with USER actor provenance; the wiki view folds subwiki.updated AND subwiki.edited latest-wins, both auditable — the Proposal pipeline is deferred), `memory.delta_committed` + `memory.core_updated` + `character.evolved` + `memory.compacted` + `cache.pruned` — the real memory store (0.16.0, M7 part 1, Rev 4 §11/§4.2/§7, owner rulings 2026-07-11: a delta is ONE curated recall note appended forever to the character's archive (the FTS5 Search Index is a projection keyed by the delta's log id, rebuilt at boot; memoryquery searches only the character's own rows); a core update is a FULL durable-core snapshot, latest per character wins — prompts inject the fixture seed + this snapshot in the stable prefix (I5); character.evolved carries a personality rewrite and/or a full goals snapshot, engine-refused for a `locked` character; memory.compacted is the CUMULATIVE summary over every delta ≤ `up_to_id` — re-running a range appends a superseding record (repair for free, nothing deleted), natural key character+up_to_id; cache.pruned is retention as a view WATERMARK (views ignore cache.appended ≤ watermark_id) — never a deletion, replay rebuilds the identical pruned view; all five emitted only by reflection-class/maintenance ledger jobs through the character's serial group), `proposal.submitted` + `proposal.resolved` — the Proposal pipeline (0.17.0, M7 part 2, Rev 4 §16, owner rulings 2026-07-11: the uniform consent object `{action, diff, rationale, proposer, approvers[]}` as a closed per-action discriminated union — `create_place` / `create_character` / `edit_wiki` / `seed_world`; submit is the ONLY durable trace until the resolve-proposal command answers — approve applies the diff atomically with proposal.resolved (applied rows carry `proposal_id` provenance), reject writes the resolved event alone, zero domain rows (I8)), `character.created` — the consent-gated seed profile (0.17.0, Rev 4 §9: the live-profile fold's event-borne seed layer; memory accrues from the first reflection), `world.seeded` — cold boot's terminal state (0.17.0, Rev 4 §9 Job 0: existence = the world is seeded; fresh worlds without it are still interviewing), `gateway.binding_established` — the first-ever sight of a (connector, messenger conversation) pair (0.17.0, Rev 4 §13: gates the once-per-binding GM onboarding push, idempotent by fold), `config.flag_set` — world flags as latest-wins folds (0.17.0, Rev 4 §15: `profiling_enabled`, default OFF — consent-first), `character.lock_set` — the user-facing evolution lock (0.17.0: folds over the seed profile's flag; gates character.evolved), `profile.updated` + `profile.deleted` — GM profiling references (0.17.0, Rev 4 §9 Job 2/§4.3: hypotheses live in a mutable side store OUTSIDE the log so GDPR delete truly erases; events carry counts and the fact of deletion, never text); `sublocation.materialized` gains optional `space` (public|private — the seeding gate's ≥1-of-each rule) + `proposal_id`, `subwiki.edited` gains optional `proposal_id` (both additive), `object.created` + `object.payload_written` + `object.moved` + `object.swept` — objects (0.18.0, M7 part 3, Rev 4 §7, owner ruling 2026-07-16: backpacks/character/user holders defer to V2, so V1 objects are sublocation-held only and public: created = materialize-on-touch (a character's gated `interact_object` atomic with its turn, or a resolve-proposal apply — narrated-but-untouched scenery never becomes data; `object_payload` optional = an empty carrier); payload_written = character authoring or the Narrator's write-on-first-read, engine-refused over an existing payload for improv writes (exactly once); moved = one pointer update, sublocation → sublocation within the scene's reach; swept = the GC tombstone — the row leaves the projection, the log stays append-only (I1); the objects table is a same-transaction fold owned by a sole-writer repository; the §16 proposal union gains action `create_object` (ProposalObjectDiff: name + holder_sublocation_id + optional object_payload) — GM-authored objects ride the existing consent seam, applied with proposal provenance and no creating scene), `marker.dropped` + `marker.instantiated` + `marker.expired` + `character.location_changed` — the living-world loop (0.19.0, M7 part 4, Rev 4 §14/§17: a marker is a LAZY intent — sublocation (materialized only) + involved characters + premise seed + game-time TTL, nothing generates until clicked; the map holds 1–5 live markers (engine top-up below the minimum, drops refused at the maximum with zero rows — I8, born-expired markers never dropped); instantiated = first click won and opened the ONE scene atomically (a racing second click joins, never twins — the version race); expired = lazy judgment against the world clock via the sweep (every clock advance + boot) or a click on an expired-but-unswept marker (refused and settled) — a skipped encounter never happened by construction (I1 tombstone semantics); character.location_changed = CRON world movement's code-class pointer update (presence-checked — never a character `in_scene`, materialized targets only, idempotent per world-cron occurrence; the map's position bubbles fold these); the markers table is a same-transaction fold owned by a sole-writer repository), `proposal.discussed` — the "Chat about this" click as a durable signal (0.20.0, the GM proposal UX contract, Rev 4 §9/§16, owner ruling 2026-07-11: the GM's next turn reads it as the tool call's interim result — stop proposing, listen; NOT a resolution — the proposal stays pending and resolvable, zero domain rows (I8); idempotent per proposal while unresolved), `character.left` + `scene.goals_updated` — the agentic scene (0.21.0, Rev 4 §6: character.left = the Narrator's character_leave, atomic with its turn.committed — releases presence for this scene only while the scene stays open; scene.goals_updated = the update_goals structured subgoal snapshot (`SceneGoalSchema` {id, text, status pending|active|done}, 1–12 goals), atomic with its turn — the engine reinjects the LATEST snapshot every Narrator turn, so a restart resumes at the exact story position; `scene.ended` end_type gains `context_limit_reached` (buttons render like rest) and `next_scene` grows the FULL §6 registration — `time_offset_hours`, `expected_participants[]`, `brief_history`, `carried_goals[]`, all optional on the wire so pre-0.21 logs parse while the tool gate requires them; `scene.started` gains optional `brief_history` + `carried_goals` — the consumed registration the engine folds in when a scene opens at the registered sublocation; `character.joined` now also arrives mid-scene from make_character, `character.location_changed` also from the Narrator's move_character (actor = the narrator, never system:world_cron))) — rows of the append-only event log, replayed via SSE `Last-Event-ID`. strictObject: own formats reject unknown keys (B5). |
| `src/stream.ts` | Ephemeral SSE frames (`hello` — protocol semver + log head + optional `app_version` (0.8.0), `stream` sentence — `call` gains `'gm'` (0.20.0): a GM reply streaming into the GM thread, `turn_id` carrying the conversation id) — display-only, never durable, never carry an SSE `id:` (B6). |
| `src/dev.ts` | Dev-channel frame union (`dev.gauges`, `dev.tool_call`, `dev.tool_rejected`) — the log-only trail for dev mode (UI Spec §2.8, Guide C11); ephemeral like `stream`, sent only to clients that opted in with `?dev=1`. `dev.tool_rejected` is the I8 trail subject: a B6-gate rejection lives only here, zero rows written. |
| `src/commands.ts` | POST command bodies + responses (`start-turn`, `interrupt-turn` — closes the envelope at the user's last-seen sentence, `end-scene`, `open-scene` — 0.8.0 adds optional `sublocation_id` to open the scene AT a known sublocation, `explore` — 0.8.0: one materialize job per fog square, idempotent, placement code-owned, `advance-time`, `paint-region`, `map-edit` — 0.9.0 Flow A: drawn polygon (world coordinates, 3–128 points) + intent (≤500 chars) + request_id, one idempotent `map_edit` ledger job, `map-click` — 0.9.0 Flow B: clicked point + request_id; the 202 answers `enter` (inside a radius/footprint — the named sublocation attached, nothing enqueued) or `classify` (one `map_click` job; the outcome arrives as map_click.resolved), `apply-update` — enqueue the verified-update staging job, `send-chat-message` — 0.11.0: DM a character (request_id-idempotent; the 202 carries `replying` + the presence answer — a character `in_scene` stores the line but generates no reply), `exit-chat` — 0.11.0: close the conversation range + enqueue its ONE reflect_chat job, `start-scene-from-chat` — 0.11.0: the startscene() bridge (place = id/name/free text; unresolved places ride scene.started as `place_request`), `start-group-chat` / `send-group-message` / `exit-group-chat` — 0.14.0: the group surface (user-started only; the 202 carries `routing`; exit answers with the per-member reflect job count), `feed-reply` — 0.15.0: the user's reply to a feed comment (request_id-idempotent, becomes the reply_id; the comment's author answers detached as social.reply_answered; text ≤ 2000), `subwiki-edit` — 0.15.0: the immediate manual wiki edit (entry ≤ 4000, appends subwiki.edited), `resolve-proposal` — 0.17.0: the approver's decision (approved applies the diff atomically, `applied` counts the appended domain events; rejected = zero domain rows; a proposal resolves once — a second resolve 409s), `set-config-flag` — 0.17.0: flip a world flag (config.flag_set fold), `set-character-lock` — 0.17.0: the user-facing evolution lock toggle, `delete-profile` — 0.17.0: the GDPR erasure right (physically deletes side-store rows, answers `removed`), `marker-click` — 0.19.0: the §1.8 marker flow (the 202 answers `instantiated` — this click won and opened the scene — or `join` — the scene already runs, enter it; an expired-but-unswept marker's click 409s `marker_expired` and settles the marker), `discuss-proposal` — 0.20.0: the "Chat about this" click as a real signal (appends proposal.discussed; the proposal stays pending; a second discuss on the same still-pending proposal 409s)), user text capped at 8 KB (B7). |
| `src/profile.ts` | `GET /v1/profile` (+ `/export`) wire shapes (0.17.0, Rev 4 §9 Job 2 guardrails): `ProfileEntry` + `UserProfileView` (entries + the `profiling_enabled` fold) — the user's own view of the GM's profiling store; the hypotheses travel only over this surface, never the event stream (the log carries counts alone). |
| `src/plugins.ts` | `GET /v1/plugins` wire shapes: `PluginInfo` (name, version, provenance incl. content sha256, zero-build asset URLs) + `PluginList`; `MapJumpDetail` — the `wl-map-jump` DOM CustomEvent detail (map connector surface, §1.14: a map plugin requests a jump, the host answers with a masked scene transition). The durable `plugin.rejected` event lives in events.ts. |
| `scripts/emit.mjs` | Emits `schemas/*.json` via `z.toJSONSchema` (`npm run protocol:emit`); CI diffs the output against the committed copies. |
| `src/*.test.ts` | Valid + extra-key + boundary fixtures per schema (B5 test rule). |

## Events consumed/emitted

This package defines shapes only; emitters/consumers are listed per event in the doc-comments (builder.md §6).

## Wire conventions

- SSE frames: `event: event` + `id: <event-log seq>` + `data: <WeltariEvent JSON>` for durable events; `event: stream` / `event: hello` / `event: dev` with no `id:` for ephemeral frames (`dev` only when the client connected with `?dev=1`).
- Field names are snake_case on the wire (`world_id`, `actor_id`, `turn_id`).
