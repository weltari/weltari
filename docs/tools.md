# tools — kill harness & consistency verifier

Purpose: the permanent crash-torture rig (Invariant I4; owner mandate: Week-1 checks become permanent CI, never throwaway scripts).

## File table

| File | What it does / talks to |
| --- | --- |
| `tools/kill-harness.mjs` | Spawns the real built server (`WELTARI_FAKE_LLM=1`, fault points on, `WELTARI_FAULT_PAUSE_MS=400`, `WELTARI_LEASE_SECONDS=2`), per cycle: proves a `Last-Event-ID` reconnect delivers exactly the missed events, drives the cycle's fault-point action, SIGKILLs, then runs the verifier offline. M2 round-robin (Brief §4 table): `mid_stream` → `between_calls` → `pre_commit` → `mid_reflection` (end-scene fan-out; the follow-up scene open is the criterion-b demonstration incl. a cross-world 202 probe) → `mid_painter` (paint-region) → `mid_cron` (advance-time) → `client_disconnect` (SSE aborted mid-narration; the turn must still commit) → `mid_update` (M3 part 2, criterion a: apply-update against a local signed release fixture, killed after verification before the pointer flip; the next cycle proves convergence — staged event + pointer flipped by the retried job — and a torn flip fails immediately) → `mid_materialize` (M4 part 2, criterion d: explore a virgin fog square, killed after both B6 gates before the reveal append; the next cycle proves the square materialized EXACTLY once — a duplicate square or a lost reveal fails) → `mid_map_edit` (M5 part 2 Flow A: a lasso edit over the common-room square, killed after both B6 gates before the sublocation.created append; the next cycle proves the edit created EXACTLY once) → `mid_map_click` (M5 part 2 Flow B: a classify click at a pre-verified outside-all-radii corner point — the 202 must say `classify`, an `enter` answer fails the cycle — killed before the map_click.resolved append; the next cycle proves the click resolved EXACTLY once) → `mid_stub_create` (M6 part 1, the in-scene creation loop: a `!query !createwild` turn commits a PARENTLESS Narrator stub — one transaction with the turn — and the kill lands in the stub-placement branch's `mid_materialize` window; the next cycle proves the stub committed exactly once, materialized exactly once at a solver-chosen frontier square, and its backdrop landed) → `mid_reflect_chat` (M6 part 2, Weltari Chat: DM Elias — he is `in_scene` in the harness scene, so the presence rule is asserted on the spot (`replying` must come back false) — then exit-chat closes the range and its reflect_chat job is killed mid-reflection; the next cycle proves the range reflected EXACTLY once) → `mid_proactive_dm` (M6 parts 3–4, proactive CRON DMs — GAME-time since part 4: the thread is reset deterministically while Elias is still in_scene (a user line stores with no reply + exit-chat closes the range), the scene is ended to free him, and a one-day advance-time crosses the daily cadence boundary — the fire it enqueues is killed mid-commit; the next cycle proves the retried fire committed its outreach — message + chat.outreach_recorded atomically — with per-occurrence uniqueness left to the verifier) → `mid_invitation_expiry` (M6 part 4, Rev 4 §7: the current scene is ended to free Elias, HE fires the meeting via the scripted `!startscene` — the character-led bridge commits scene.started with its 6 h game-time invitation — then a 12 h advance-time crosses the window and the expiry sweep is killed before the scene.expired + cache.appended pair commits; the next cycle proves the BOOT sweep expired it exactly once, pair atomicity left to the verifier's 4j block) → `mid_social_post` (M6 part 5, Rev 4 §12 — the Feed: the current scene is ended so BOTH fixture characters are available and the salted poster pick is deterministic, then a 12 h advance-time crosses exactly one social cadence boundary (720 min at the default 2 posts/game day) and the fire is killed after generation, before the post + poster-CACHE + reaction-job transaction; the next cycle proves the retried fire committed EXACTLY one social.post_committed, per-occurrence uniqueness and reaction/reply keys left to the verifier's 4k block) → `mid_memory_commit` (M7 part 1, Rev 4 §11 — the memory store: same seed as mid_reflection, but the kill lands inside the NEW memory-commit window — deltas/core gated, the atomic reflection + CACHE + memory-events append not yet written; the next cycle proves the retried job committed EXACTLY one reflection AND exactly one delta set — the fake's two scene deltas — for the character+scene) → `mid_compaction` (M7 part 1: the cycle grows Elias's uncompacted archive to just below the 16-delta trigger via turn + end-scene rounds — each reflection adds two deltas, earlier cycles' deltas count — then the FINAL round crosses the threshold while the harness already waits on the fault line, so the kill lands inside the compaction pass's own commit window; the next cycle proves the retried pass committed its memory.compacted, per-range uniqueness left to the verifier's 4l block) → `mid_proposal_apply` (M7 part 2, Rev 4 §16 — the Proposal pipeline: the GM proposes a place through its own conversation via the scripted `!proposeplace`, the card's detached commit is polled from the log, and the user's APPROVE is killed inside the apply window — gates passed, the proposal.resolved + applied-rows + backdrop-job transaction not yet written; the next cycle proves NOTHING half-applied (a resolved-less row is a torn apply) and a fresh resolve applies the card EXACTLY once, resolution↔rows pairing left to the verifier's 4m block) → `mid_profile_analysis` (M7 part 2, Rev 4 §9 Job 2: profiling is switched ON (set-config-flag, idempotent), a one-message Elias range closes via the mid_reflect_chat shape and enqueues the analysis job, which is killed after generation + gating, before the side-store rows + profile.updated transaction; the next cycle proves the retried job committed EXACTLY one hypothesis set — the fake's two — for the (actor, context)) → `mid_object_gc` (M7 part 3, Rev 4 §7 — objects: a committed `!obj` turn materializes a payload-less stray, end-scene enqueues the object_gc sweep, and the kill lands inside its tombstone window; the next cycle proves the retried sweep tombstoned the stray EXACTLY once — row gone, ONE object.swept, the object.created still in the log (I1) — with sweep legality left to the verifier's 4n block) → `mid_marker_sweep` (M7 part 4, Rev 4 §14 — the living-world loop: boot guarantees ≥ 1 live marker (the top-up floor); a 1-day advance-time pushes the world clock past its 180-game-minute TTL and the sweep is killed before its marker.expired commit; the next cycle proves the BOOT sweep settled it EXACTLY once and the floor is back — marker legality left to the verifier's 4o block) → `mid_marker_click` (M7 part 4: a live marker's click is killed inside the instantiate window — everything decided, the marker.instantiated + full scene-open transaction not yet written; the next cycle proves the pair is ATOMIC: still-live with zero scenes, or instantiated exactly once WITH its one scene.started — never a torn half) → `mid_marker_topup` (M7 part 4: live markers are clicked down one by one — every click path shrinks the live set — and the click that empties it triggers the engine top-up, killed before its generated marker.dropped commits; the next cycle proves the boot top-up restored 1 ≤ live ≤ 5) → `mid_gm_followup` (the GM proposal UX contract, 0.20.0: the GM proposes a place through its own conversation, the user's APPROVE commits for real, and the kill lands inside the DETACHED durable tool-result turn's commit window — the follow-up generated, the chat.message_committed with its deterministic id `gm-followup-<proposal_id>` not yet appended; the next cycle proves the BOOT sweep committed the follow-up EXACTLY once — a duplicate is a hard failure, key discipline left to the verifier's 4q block). M4 part 2: fresh worlds boot scene-less (the splash is the entry surface), so the harness opens its first scene through open-scene like any client. The release fixture reuses the compiled test signers (`tests/dist/helpers/minisign.js` + `tar.js`) on a local HTTP server with a fresh version per mid_update cycle. `CYCLES` env: 25 per PR, 100 nightly. Works on Windows (`kill('SIGKILL')` = TerminateProcess); M3 hardening for Windows ephemeral-port pressure: per-cycle server-port rotation + persistent `EADDRINUSE` retry. Week 19 hardening (found by the first on-machine CYCLES=100): the 400 ms fault pause now applies ONLY to the cycle's kill needle (`WELTARI_FAULT_TARGET`, set per spawn; mid_stub_create's needle is mid_materialize) — untargeted pauses inflated accumulated world-cron replay bursts (a +1-day advance leaves ~15 due occurrences; rapid-kill cycles roll the backlog forward) until the single runner starved the round-2 mid_reflection wait. Kill windows are unchanged — the hunted point still holds the full pause; unset target = the old pause-everywhere behavior. → `mid_charactercall` (0.21.0, the agentic scene: a loop turn — a goals marker + the default determine_who_next/charactercall of Elias — is killed INSIDE the charactercall executor, declaration consumed and staged effects in memory; the next cycle proves the whole turn VOIDED — started-without-committed, zero partial cast/goal rows — with the goals↔turn atomicity and cast discipline left to the verifier's 4r block). 26 points: full coverage per run needs CYCLES=26. |
| `tools/m3-plugin-proof.mjs` | M3 criteria (a) + (d): `PROOF_MAIN` / `PROOF_PLUGINS_DIR` / `PROOF_NODE` env overrides retarget it at an extracted package (part-2 criterion d measures the shipped artifact). Authors a drop-in plugin (theme + custom element + connector, plain files), boots the real server with `plugins/` (incl. wl-map), verifies `GET /v1/plugins` lists all three capabilities with the computed provenance sha256, fetches the zero-build assets, samples idle RSS from the server's gauges (< 170 MB), then flips one byte and proves the B10 refusal on restart (`plugin.rejected` durable, assets 404, app boots without it). Cleans up after itself. |
| `tools/verify-consistency.mjs` | Offline checks after each kill: `PRAGMA integrity_check`; event ids strictly increasing + unique; payloads all JSON; envelope discipline (≤1 commit per turn, no commit without start — started-without-committed is the expected voided-turn shape, B6); scene.ended fan-out atomicity (event ⇒ all its job rows); cold-path outcome events unique per natural key (reflection/world-agent/world-cron/painter, plus M6 part 1: stub_created per stub id, materialized per stub id AND per square — kill-retry stayed idempotent); create-tool atomicity (a sublocation.stub_created ⇒ its backdrop paint job row, parentless ⇒ also its materialize job row); M6 part 2 chat keys (chat.message_committed per conversation+message_id, chat.ended + reflect_chat.committed per conversation+range_end, subwiki.updated per scene+sublocation) + chat-end atomicity (chat.ended ⇒ its reflect_chat job row); M6 part 3 outreach checks (block 4i): chat.outreach_recorded unique per world+occurrence, chat.thread_frozen unique per conversation+message, every outreach ⇒ its delivered message exists (torn-transaction check), no freeze below the 3-unanswered cap; M6 part 4 invitation checks (block 4j): scene.expired unique per scene, only over a never-entered invitation scene, never coexisting with a scene.ended, and always paired with its hardcoded absence cache entry (torn-transaction check); M6 part 4 group checks: chat.group_message_committed unique per conversation+message, chat.group_ended unique per range and always carrying one reflect_chat job per member (torn-transaction check); M6 part 5 feed checks (block 4k): social.post_committed unique per world+occurrence, one reaction decision per post+character (body iff comment), one answer per user reply (in_reply_to), and every reaction/reply names a post that exists; M7 part 1 memory checks (block 4l): delta sets capped at 3 PER COMMITTED REFLECTION for the (character, origin, context) — a chat conversation closes many ranges over its life, so the allowance scales with its reflect_chat count (M7 part 2 fix) — and a delta without any reflection is a torn transaction; memory.compacted unique per (character, up_to_id) with up_to_id below the record's own id; cache.pruned watermarks below their own id; the FTS Search Index exactly mirrors the delta events (row-for-row, offline); M7 part 2 GM checks (block 4m): every proposal resolves at most once and only if it exists; a REJECTED proposal has ZERO rows carrying its id (I8); an approved create_place/seed_world's applied rows exist and match the diff exactly (places count, characters count, one world.seeded); applied rows NEVER exist without an approval (torn apply); at most one world.seeded per world; at most one gateway binding per (connector, conversation); profile.updated unique per (actor, context) and its side-store rows exist unless a later profile.deleted erased the actor's store (the sanctioned mutation); 4m also counts approved create_object rows (M7 part 3: exactly one object.created per approval); M7 part 3 object checks (block 4n): the objects table mirrors the fold of the object.* events EXACTLY (row-for-row, offline — the row rides the append's transaction, boot rebuilds); every object event references an object alive at that point (created once, nothing after swept); (name, holder) unique among live rows per world; a swept object was a legal stray — payload-less, scene-created, never touched outside its creating scene, and its creating scene ENDED before the tombstone; M7 part 4 marker checks (block 4o): the markers table mirrors the fold of the marker.* events EXACTLY (terminal rows stay by design); state walks dropped → instantiated | expired only; the live set never exceeds the ceiling (5) at any drop; the expiry stamp equals dropped_at + ttl; no marker was ever dropped born-expired (scheduled + ttl behind the world clock at drop); every instantiation's scene.started exists; every expiry was judged at or after the deadline; M7 part 4 movement checks (block 4p): every character.location_changed carries the world-cron system actor, lands on a sublocation MATERIALIZED at that point (stubs never receive CRON traffic), never moves a character who was in an open scene, and chains exactly (from = the previous folded location, absent only on the first move); the GM UX-contract checks (block 4q): at most ONE GM follow-up message per resolution (`gm-followup-<proposal_id>`) and per discuss signal (`gm-discuss-<proposal_id>`) — a missing follow-up is legal mid-kill (the boot sweep heals it), a duplicate or an orphan never is; every follow-up is a GM character line whose outcome exists and precedes it in the log; a proposal is discussed at most once, only while it existed unresolved; world clock monotonic per world; painter hash check (`<images-dir>` arg: every painter.completed's file exists and matches its sha256 — zero corrupted images); update.staged unique per version + pointer discipline (`<versions-dir>` arg: a `current` pointer must name a complete version dir — no torn flips, B12); ledger states legal, running rows leased. Exit 1 = CI fails. 0.21.0: 4p becomes the WORLD-movement block — movers are system:world_cron (materialized targets only) AND char:narrator (move_character: any sublocation known at that point, stubs included), with character.left releasing a character from an open scene mid-play; NEW block 4r (the agentic scene): every scene.goals_updated names a turn.committed preceding it (one transaction), per-scene casts never tear (no double character.joined, no character.left of an absent member), and every brief_history-carrying scene.started was consumed from a real earlier registration in that world. |
| `tools/m2-rss-check.mjs` | M2 criterion (c): spawns the server with the ~50K-token prefix and 200 ms gauges, drives open-scene → turn → end-scene fan-out + painter composite + 3-day time-skip, takes peak `rss_mb` from the server's own gauge lines. Exit 1 at ≥ 256 MB. |

| `tools/cache-hit-check.mjs` | The success-criteria / nightly real-provider runner (Guide §0.14): spawns the real server (`LOG_LEVEL=debug`, `WELTARI_PREFIX_TOKENS` default 50000), opens the measured scene (fresh worlds boot scene-less since M4 part 2), plays `TURNS` (default 20) consecutive turns over the live SSE stream, measures time-to-first-sentence per turn, reads per-call `cached_tokens` from the debug log, samples idle RSS at the end. Voided turns (provider 429/5xx) are retried after a backoff. Requires `OPENROUTER_API_KEY` (env only). Exit 0 = criteria a, b, e all pass. |

| `tools/check-tests-accompany.mjs` | E2 gate: `git diff` against the PR base — a new `apps/server/src` or `packages/*/src` file with no test file added/modified in the same range exits 1. Runs in the PR-only `tests-accompany` CI job. |

| `tools/update-fixture.mjs` | Manual dev fixture (M4, Config update surface): the kill harness's signed-release trio as a standalone local server — `node tools/update-fixture.mjs [version] [port]` prints the `WELTARI_UPDATE_RELEASES_URL` + `WELTARI_UPDATE_PUBKEY` to start the server with; the Config page then shows the real update.available → Apply → update.staged round-trip. Fresh keypair per run — nothing it signs verifies anywhere else. |

| `tools/m5-map-qa.mjs` | M5 part-1 criterion (c): the VLM map-QA spot check. `--image <png> --name --description` + `OPENROUTER_API_KEY` → one real multimodal call through the shipped seam (`dist/llm/vlm.js`, `WELTARI_VLM_MODEL` default `google/gemini-3.5-flash`), reply gated exactly like the server would (parseLlmJson → validateAt over a strict verdict schema — reject, never repair). Owns no database: a rejected/garbage reply writes zero rows by construction. Exit 0 = gated verdict says the described sublocation is plausibly visible; 1 = gated verdict says not visible; 2 = gate rejection or provider failure. |

## CI wiring

- `.github/workflows/ci.yml` → `kill-harness` job, `CYCLES=25`, every push/PR.
- `.github/workflows/nightly.yml` → `CYCLES=100` at 03:30 UTC + manual dispatch. The nightly real-provider cache-hit check joins here (see file comment).

## Why the fault pause

`FAULT_POINT:<name>` is printed, the harness reacts in milliseconds — but a microtask can commit first. `WELTARI_FAULT_PAUSE_MS` holds the engine at `between_calls`/`pre_commit` so the SIGKILL reliably lands inside the window; `mid_stream` needs no hold (many event-loop yields during streaming, and nothing durable exists in that phase either way).
