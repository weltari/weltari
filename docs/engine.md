# engine — apps/server/src/engine

Purpose: engine-owned truth and prompt assembly. No wall-clock reads here (lint-enforced, Guide A16); everything is a pure function of injected inputs so tests are deterministic and the stable prefix is byte-stable.

## Contract

- Inputs: character profiles, scene context (rendered world-clock text, transcript, external text).
- Outputs: `{ stablePrefix, dynamicTail }` — the I5 structural guard; events via `EventSink` (append-then-publish).
- Never: interpolate dynamic or external text into the stable prefix (Brief §2.6); treat external text as instructions (B14); read `Date.now()`/`new Date()`.

## File table

| File | What it does / talks to |
| --- | --- |
| `context-assembler.ts` | `assembleContext(profile, scene)` — stable-first order (skills → personality → memory core → goals), dynamic tail last; external text (wiki/transcript/player) tail-only inside `<external source=…>` wrappers with angle brackets neutralized so it cannot close its own wrapper. M7 part 1: `CharacterProfile.memory_core` is the immutable SEED and `locked` freezes personality/goals against evolution — the durable state lays on top via [memory.ts](#file-table). |
| `memory.ts` | The character memory fold (M7 part 1, Rev 4 §11/§17): `memoryStateOf` — one character's durable memory as a pure projection (latest `memory.core_updated` snapshot, latest `character.evolved` personality/goals per field, latest `memory.compacted` per range, every delta ascending); `liveProfile` — the seed profile with that state laid on top (memory core = seed + snapshot; personality/goals replaced latest-wins) — EVERY character-class call site assembles from this (chat, group chat, scene turns, reflections, proactive DMs, all three social handlers), so the call after a core update provably injects it and the prefix stays byte-stable between memory commits (I5); `archiveView` — the compaction-preferring read path (latest summary + deltas newer than it; deltas behind the summary stay in the log and the Search Index); `gateReflectionMemory` — gate 2 for the reflection memory outputs (delta cap 3, last-core-wins, the `locked` refusal, prefix-hygiene sanitization via `sanitizeMemoryText`); `compactionDue`/`enqueueCompactionIfDue` — the compaction trigger (window `MEMORY_COMPACT_KEEP` = 10, trip at 16 uncompacted; deliberately outside the commit transaction — world-inert, the boot sweep heals a missed enqueue); `archiveRecapText` — the archive POINTER (owner ruling 2026-07-11: the main memory carries a summary of the sub-memory so the agent can judge whether retrieval is worthwhile): the latest compaction summary + counts of what stands behind it, injected into the DYNAMIC TAIL of chat replies and character scene turns (reflection-authored text is data — B14; never the prefix). Memory UPDATE semantics, for the record: the core snapshot and personality/goals evolution are full REPLACEMENTS (a changed stance supersedes the old one); deltas are the append-only diary underneath; nothing anywhere can delete (owner ruling: no memory deletion in V1 — enforced by the append-only log itself, I1). |
| `scene-lifecycle.ts` | `endScene`: scene.ended + one reflection job per participating character (derived from the scene's committed turns) + one World Agent job (`serial_group world_agent:<world>`), all in ONE WriteGate transaction (Brief §2.4); bus publish after commit. `openScene`: blocks (409) only on this world's World-Agent jobs + this scene's participants' reflections (Brief §4, criterion b); M4: appends one `character.joined` per KNOWN participant atomically with scene.started (the roster projection — unknown ids are skipped with a warn, B6 ethos), published after commit in append order. M4 part 2: optional `sublocation_id` opens the scene AT a known sublocation — registry-gated (`unknown_sublocation` 409), the `sublocation.changed` appends in the same transaction. M3: the atomicity core is extracted as `appendSceneEndWithFanOut` (runs inside a caller's transaction) so the Narrator's end_scene tool commits scene.ended + jobs in the SAME transaction as turn.committed. M6 part 1: `SceneEndRequest.next_scene` rides into the scene.ended payload (the "Jump to the next scene" registration); open-scene AT a Narrator stub works (position-less until materialized) and the `sublocation.changed` it appends carries `backdrop_path` when the stub's backdrop already landed. M6 part 2: `OpenSceneRequest` extends the command with optional `premise`/`place_request` (the chat→scene handoff, Rev 4 §8) — they ride scene.started; the HTTP open-scene command never sets them, the startscene() bridge does; scene-turn folds them into the Narrator's FIRST turn (premise until a turn commits, place request until the scene moves) inside the player-wrapped external block (B14) plus a resolve-it-THIS-turn instruction. |
| `scene-tools.ts` | Gate 2 of the B6 double gate: validates shape-valid tool calls against game state (sublocation exists ∧ differs from current; character present ∧ pose in their art set; scene open ∧ not already ending). Valid effects are STAGED — the turn engine appends their durable events atomically with turn.committed; every rejection is a `dev.tool_rejected` trail frame with zero rows written (I8). M6 part 2: rejections are no longer trail-only — the turn engine's mid-call gate executor (`LlmCall.gate`, [llm.md](llm.md)) returns the same refusal string to the MODEL as a tool ERROR, so it can self-correct in the same turn. M4 part 2: the sublocation list comes from the registry (fresh per turn), so materialized squares are enterable without a restart. M6 part 1 (Rev 4 §6): `create_sublocation` joins — deterministic stub ids (`subloc:stub-<slug>`), the did-you-mean near-duplicate resolver (normalized-name collision → rejection naming the existing id), the flat-parent rule (an interior's parent must itself be parentless), and the parentless query-first rule: a parentless create is refused with Rev 4's fixed instruction unless the stage's `querySublocations` executor ran mode `parentless` THIS turn (V1: same-turn ⊂ Rev 4's same-scene — strictly stricter). Staged creates are visible to same-turn `change_sublocation` and `end_scene.next_scene` (the creation loop: create → move → continue in one reply); an interrupt discards them all. `querySublocations` also serves modes children/search — read-only, never staged. |
| `sublocations.ts` | The sublocation registry (M4 part 2): known = fixture trio ∪ `sublocation.materialized` ∪ `sublocation.created` (M5 part 2 Flow A — those carry a `footprint` polygon) ∪ `map_click.resolved` with outcome `created` (M5 part 2 Flow B — the resolved event IS the persistent spawn's row) ∪ `sublocation.stub_created` (M6 part 1 — Narrator stubs: interiors inherit the parent's anchor and carry `parent_id`; parentless stubs stay position-less until their materialization overwrites the entry under the SAME id) events per world — ONE projection read by the change_sublocation gate, open-scene's sublocation gate and explore's occupancy gate. Square math for the 8×8 fog grid (`squareOf`/`squareCenter`, protocol `MAP_FOG_GRID`); ids are deterministic per square (`subloc:sq-<col>-<row>`) so retries can never mint twins. Square occupancy counts only fixture/materialized rows: Flow-A/B sublocations are sub-square features, interiors and unmaterialized stubs have no map presence at all — none block an Explore, anchor a VLM label or catch a radius click. Flow-B hit tests: `SUBLOCATION_RADIUS` (half a fog square) + `sublocationNear` (footprint containment wins, else nearest anchor within the radius) — the engine's authoritative copy of the rule the default map plugin mirrors. M6 part 1 additions: `solveFrontierSquare` (the code-owned placement solver, Rev 4 §14: nearest free square touching the explored 8-neighborhood, deterministic tie-break; full map → undefined, the stub stays map-less) and `latestBackdropPath` (the newest `painter.completed` for `backdrop:<sublocation_id>` — the event is the truth). |
| `chat.ts` | Weltari Chat part one (M6 part 2, Rev 4 §8): the DM core. Conversations are projections of `chat.message_committed`/`chat.ended` on the ONE event stream (stable `conversation_id` = `chat:<actor_id>:<character_id>`); the user line commits at the seam, the reply generates DETACHED (kind `chat`, toolset `chat`, no streaming in V1) and commits atomically with its mandatory chat-origin CACHE line — a kill mid-generation loses one reply, nothing else (B6). `presenceOf` = the presence projection (in_scene while a joined scene is open; Rev 4 §4 structured-state rule): in_scene ⇒ the message stores, NO reply generates; WORLD-scoped since M6 part 3 (a scene left open in another world — the harness's cross-world probe was the reproducer — must never freeze this world's DMs). Duplicate `request_id` = silent no-op; a message during generation queues ONE nudged follow-up, never a race. `exitChat`/`sweepIdle` close a range: `chat.ended` + its ONE `reflect_chat` job in one transaction, keyed `reflect_chat:<conversation>:<range_end_id>`; the idle horizon is an injected `idleCutoffIso` (A16 — Zulu ISO strings compare lexicographically), owner default 30 min via `WELTARI_CHAT_IDLE_MINUTES`. Chat never changes the world. The startscene() bridge (Rev 4 §8 — THE way back into scenes): the character's `startscene` tool (after its reply commits) or the user's start-scene-from-chat command → place resolved against the known-sublocations registry (id or slugified-name match → the scene opens AT it; no match → the free text rides scene.started as `place_request` for the Narrator's standard create workflow, query-first included), scene opened WITH the character (presence flips in_scene = the reservation), then the chat range closes (reason `startscene`). Scene open and chat close are two transactions on purpose — a kill between them leaves the conversation open and the idle sweep heals it. M6 part 3 (owner ruling 2026-07-09: meeting is conversational and character-led, never a button): `CHAT_CONDUCT_SKILL` — a stable constant appended to the profile skills for chat calls only (prefix stays byte-identical, I5) — teaches the negotiation (gather the place before firing, one question at a time, don't re-ask what the User volunteered), the firing rule (the character calls `startscene` ITSELF; the User cannot), and the V1 limits declined in-character (no char-to-char texting/meetings, no acting on the User's behalf, no self-exit). The bridge now runs the one-active-scene transition (the debug-session carry-over, same rule as the web's map jumps): any scene still open is ended FIRST (full fan-out), then the open retries bounded (`bridgeRetryDelayMs` × 20) while that fan-out blocks it; if it never drains the bridge gives up as a value (`blocked_on_pending_jobs`) and the chat stays open. `startSceneFromChat` is async accordingly. Chat replies offer `wikiquery`/`sessionquery` as mid-call queries ([chat-queries.ts row](#file-table)); each execution publishes a `dev.tool_call` frame on the injected `devBus` (C11 — criterion d's visible trail). M6 part 4 (0.13.0, Rev 4 §7, owner rulings 2026-07-10/11): the character-fired path carries the character's REQUIRED game-time `wait_hours` into `scene.started`'s `invitation` (the engine stamps `expires_at_game` at open; the dev button never sets one — a user firing the scene IS showing up); a malformed `startscene` runs the critical-tool correction loop — the whole reply regenerates with a hardcoded `## Correction` (nothing committed yet, so a retry replaces), ceiling `STARTSCENE_RETRY_ATTEMPTS` = 10, then the fire rolls back and the reply commits with a hardcoded `chat.notice` red line (`startscene_rejected`); `presenceOf` and the open-scene fold treat `scene.expired` exactly like `scene.ended` (the release half of invitation expiry). |
| `group-chat.ts` | Group chats (M6 part 4, Rev 4 §8, 0.14.0): USER-started only — characters cannot fire group chats and CRON never posts into them. `startGroup` (idempotent per request_id; conversation `group:<actor>:<request_id>`; members validated, fixed at start) · `sendMessage` (user line durable at the seam; the ROUTER ROUND runs detached: per step the Group-chat Narrator — kind `group_route`, toolset `group_router`, router-class — picks who speaks next via `route`/`endsubsession` tool calls; its text is DROPPED un-surfaced (NO narration, decisions are dev-trail frames); the ENGINE validates the pick (member ∧ available) and enforces the turn budget — `WELTARI_GROUP_TURN_BUDGET`, owner default 3 — so the router cannot ping-pong past it; each routed member replies through the normal chat shape and commits `chat.group_message_committed` + its CACHE line; a routed member may `stay_silent`) · `exitGroup`/ENDSUBSESSION close the range: `chat.group_ended` + exactly ONE `reflect_chat` job PER MEMBER in one transaction (keys `reflect_chat:<conv>:<member>:<range>`). Groups never change the world; `startscene` from a group is ignored un-honored (the bridge is a DM surface — group scenes are V2). |
| `cache.ts` | The CACHE store first slice (M6 part 2, Rev 4 §11): a PROJECTION of `cache.appended` events — per-character, append-only, "latest" is a view never a slot. `latestPerOrigin` = the cross-context catch-up read (latest scene line + latest chat line + latest social line since M6 part 5, each its own lane — a chat recap or a feed comment can never shadow a scene experience); `cacheRecapText` renders the chat prompt's tail block FRESH every call (owner decision 2026-07-09); `capCacheLine` normalizes the character-authored one-liner (engine writes every structured field). Writers: the reflection handler (origin `scene`, this slice's stand-in until the C-Module writes in-scene), the chat engine (origin `chat`, every reply), and the social handlers (origin `social`, M6 part 5 — poster and reactor halves of the two-sided memory write). M7 part 1 retention (Rev 4 §11): `cache.pruned` is a view WATERMARK every read respects (`CACHE_KEEP_DEFAULT` = 50, env `WELTARI_CACHE_KEEP`); `cachePruneDue`/`enqueueCachePruneIfDue` feed the cache_prune job ([ledger.md](ledger.md)) from the reflection commits and the boot sweep. |
| `explore.ts` | The explore command seam (UI Spec §1.8): gates world-exists + square-empty (409 `world_not_found`/`square_occupied`), enqueues ONE `materialize` ledger job keyed by the square (I3: duplicate clicks are silent no-ops that still 202), kicks the runner so the spinner window tracks generation latency, not the 1 s poll. |
| `map-edit.ts` | The Flow-A command seam (Rev 4 §14, M5 part 2): gates world-exists + centroid-on-explored-ground (409 `world_not_found`/`unexplored_ground`), appends the durable `map_edit.requested` intent (the client's lock-overlay anchor; not re-appended for a duplicate request_id), enqueues ONE `map_edit` ledger job keyed by request_id (I3), kicks the runner. All geometry via painter-owned `editGeometry` ([painter.md](painter.md)). |
| `map-click.ts` | The Flow-B command seam (Rev 4 §14, M5 part 2). Step 1 — the radius check — answers HERE, synchronously: a click inside a known footprint or `SUBLOCATION_RADIUS` enters that sublocation with ZERO model calls and ZERO rows (202 `enter`); a fog click is refused (409 `unexplored_ground` — Explore owns fog); only a click outside all radii enqueues ONE `map_click` job keyed by request_id (202 `classify`, I3). |
| `chat-queries.ts` | The chat query escalation executors (M6 part 3, Rev 4 §11: "latest-per-origin instantly; escalate to scene-query → session read for specifics"): `runWikiquery` — the wiki read over the sublocation registry merged with the SUBWIKI projection (latest of `subwiki.updated` AND `subwiki.edited` per sublocation wins since M6 part 5 — a manual edit is wiki truth for every read the moment it lands; tier 2 is a relevance scope, not secrecy); `runSessionquery` — scene-query (tier 3): finds a past scene via its World-Agent recap and returns recap + final transcript lines, PARTICIPATION-GATED structurally (only scenes whose `scene.ended` participants include the character are searchable; a scene without a recap yet is not searchable). Read-only mid-call executors on the proven `LlmCall.queries` seam; malformed input answers with an error string, never throws. All-tokens keyword match — "the …" can never hit everything. M7 part 1: `runMemoryquery` — the memory deep dive (Rev 4 §11 tier 4): BM25 over the character's OWN deltas via the FTS5 Search Index ([storage.md](storage.md)); participation-gated twice over (the executor is bound to one character id at the call site AND the index filters on the character column). Offered mid-call to chat replies AND character scene turns (owner ruling 2026-07-11: chat + scenes). |
| `social.ts` | The Feed's engine-side folds (M6 part 5, Rev 4 §12): `acquaintancesOf` — the delivery rule as a pure world-scoped fold (owner ruling 2026-07-11: shared scene session via `character.joined` grouped by scene OR shared group chat via `chat.group_started.member_ids` = having met; sorted output so the reaction-cap pick is deterministic; never contains self); `pickReactionCandidates` — the deterministic salted-hash ranking that picks which ≤cap recipients get the ONE reaction decision (different posts rotate through different subsets; a kill-retry re-derives the SAME picks); `SOCIAL_CONDUCT_SKILL` — the stable feed conduct constant (I5: the medium, the no-promises rule — the feed has no startscene tool, comments never thread between characters — and the CACHE duty); `SOCIAL_POST_SKIP_CAP` = 10 (the §12 per-skip ceiling, freshest window survives). No clock reads, no LLM. |
| `feed.ts` | The feed-reply command seam (M6 part 5, owner ruling 2026-07-11): the user replies to a CHARACTER COMMENT on a feed post — a feed-local thread under that comment, never routed into Weltari Chat. Gates: the target must exist and be kind `comment` (409 `unknown_comment`/`not_a_comment`); idempotent per request_id (duplicate = silent 202, no re-kick). Commits `social.reply_posted` (USER actor) + the `social_reply` answer job in ONE transaction (`appendManyWithJobs`; serial group = the author's social mailbox lane), kicks the runner. Uncapped — user-triggered spend. |
| `wiki-edit.ts` | The manual wiki edit seam (M6 part 5, owner ruling 2026-07-11: edits apply IMMEDIATELY — the Proposal pipeline is deferred to M7): registry-gated (or an existing subwiki entry), appends ONE `subwiki.edited` with USER actor provenance per flush. The wiki view folds `subwiki.updated` AND `subwiki.edited` latest-wins — a later World Agent pass may supersede the text but never silently (both stay in the log, criterion d). |
| `outreach.ts` | Proactive-DM projections (M6 part 3, Rev 4 §8): `outreachState` — the unanswered counter as a pure fold (outreaches after the user's last line; a reply resets AND thaws by construction, no reset event exists), the freeze cap (`OUTREACH_FREEZE_CAP` = 3), `outreachEligible` — the three owner rules (frozen → never; answered → only when QUIET, no open range; unanswered → growing backoff base × 2^n), and `pickIndex` — the deterministic FNV "random in V1" pick (a kill-retry can never switch targets). No clock reads: the fire time arrives as the scheduler occurrence in the job payload. |
| `proposals.ts` | The Proposal pipeline (M7 part 2, Rev 4 §16): the uniform consent flow, agent-agnostic (the GM is merely its first proposer). `submit` re-shapes a gate-1-validated tool diff through the WIRE union via `validateAt` (the one place the action↔diff pairing is enforced) then gate-2 checks references against world state (name/id dedup via deterministic ids `subloc:gm-<slug>` / `char:<slug>`; `seed_world` additionally: world unseeded, ≥1 public + ≥1 private space, unique names) and appends `proposal.submitted` — NOTHING else is durable at submit. `resolve` (the approver's decision, approver-list gated): reject appends `proposal.resolved` alone (zero domain rows, I8); approve re-runs gate 2 against CURRENT state, assembles the deterministic apply plan (places → `sublocation.materialized` rows on frontier-solved squares + opening `subwiki.edited` + backdrop paint jobs; characters → `character.created`; wiki edits → `subwiki.edited`; seeds additionally stamp `world.seeded`), then commits resolved + plan + jobs in ONE `appendManyWithJobs` transaction behind the standing triad (natural key proposal_id, eager already-resolved check, fused re-check after the `mid_proposal_apply` fault line). Applied rows carry the PROPOSER as actor + `proposal_id` provenance; the resolution carries the approver's. `pendingProposalsOf` is the frontend's card projection. |
| `gm.ts` | The GM persona (M7 part 2, Rev 4 §9): `GM_CHARACTER_ID` (`char:gm`) + `buildGmProfile()` — a deliberately CONSTANT CharacterProfile (skills teach interviewing, the consent contract, and product self-knowledge; the GM's knowledge of the user arrives via the profiling loop, never by mutating this), so the context assembler builds its prompt like every agent's and the GM's stable prefix is byte-stable across the interview's many short turns (I5). Not a character: no CACHE, no reflection, no presence. |
| `characters.ts` | The character registry fold (M7 part 2, Rev 4 §9): seed (fixture/config) profiles ∪ every `character.created` event (consent-gated applies are the only writer), with the user's `character.lock_set` overlaid latest-wins on the `locked` flag. A created character is a seed profile like any fixture — `liveProfile` folds durable memory on top identically (M7 part 1: multi-character by construction; week 18's `make_character` appends the same event). |
| `fault-points.ts` | The `FaultPoint` union — the kill-harness contract (I4): Week-1 three + M2's `mid_reflection`/`mid_painter`/`mid_cron` + M3's `mid_update` + M4's `mid_materialize` + M5 part 2's `mid_map_edit`/`mid_map_click` + M6's `mid_reflect_chat`/`mid_proactive_dm` + M7's `mid_memory_commit`/`mid_compaction`/`mid_proposal_apply`. |
| `world-clock.ts` | The engine-owned fictional WorldClock: current time = projection of `world.time_advanced` events (epoch `2000-01-01T06:00Z`); M6 part 3: the fold is also exported standalone as `worldTimeOf` (the proactive_dm handler's game_time stamp). `advanceTime` computes due world-cron occurrences (fictional-calendar math delegated to `ledger/scheduler.js` — A16), enqueues code-class first then the newest ≤budget LLM-class (default 10), all atomic with the time event; `kick` lets main drain the runner immediately (code-class = instant, Brief §4). |
| `invitation.ts` | Invitation expiry (M6 part 4, Rev 4 §7, owner rulings 2026-07-10/11): `pendingInvitations` — the pure fold over open, never-entered invitation scenes ("never entered" = no `turn.committed`; visiting the scene page fires the first turn, so showing up counts); `createInvitationExpiry(...).expireDue(world)` — the LAZY judgment against the WORLD clock (never wall time: while the user is away the clock is paused and the character has fictionally waited no time). Runs after every clock advance (the only way the deadline can newly pass — main wraps the advance-time dep) and once at boot per `pendingInvitationWorlds` (recovery path = startup path; heals a kill inside `mid_invitation_expiry`). On expiry ONE transaction appends `scene.expired` + the HARDCODED `cache.appended` absence entry (never an LLM call; day-stamped with the game clock) behind a fused re-check — natural key scene_id, kill-retry and racing-sweep safe. Presence releases via the folds treating `scene.expired` like `scene.ended`; the character complains on its next trigger because the absence entry is its latest scene-origin CACHE line. |
| `fixture/rainy-inn.ts` | + `FIXTURE_WORLD_CRON`: `lamplighter` (code, fictional 06:00) and `evening_rumor` (LLM, fictional 18:00). |
| `fixture/rainy-inn.ts` | Deterministic Week-1 fixture world: Elias profile (`buildEliasProfile(targetPrefixTokens)` — sizes the memory core up to the ~50K-token success-criteria prefix), Narrator profile, lore generator (pure function, no randomness). M3: `FIXTURE_SUBLOCATIONS` (common_room/cellar/shrine — clients render placeholder backdrops per id) and `FIXTURE_ART_SETS` (Elias: neutral/smile/worried/working). M4 part 2: the trio carries stub descriptions; the fresh-world seed emits them as `sublocation.materialized` (the map starts with three explored squares) and NO scene auto-opens anymore — the splash (wireframe 03) is the entry surface; the gateway opens its echo scene on demand. |
| `event-sink.ts` | Documented under [http.md](http.md) (append-then-publish). |

## Deviations recorded

- ~~The root `fixtures/` directory is deferred~~ — resolved in M4:
  `fixtures/example-world/` + loader exist for row inspection
  (builder.md §4.3). The engine's own fixture world still ships as code here
  (type-safe imports); that split is deliberate.

## Tests

Invariants I5: `tests/invariants/prompt-prefix/context-assembler.test.ts` — byte-identical across calls, dynamic-only changes leave the prefix untouched, hostile-injection fixture, wrapper-escape neutralization, deterministic ~50K-token fixture scaling. `tests/invariants/prompt-prefix/live-profile.test.ts` (M7 part 1) — the memory fold's half: same log state ⇒ identical bytes; a core update changes the prefix exactly between calls, seed lines ahead of the snapshot; another character's memory events leave the prefix untouched; evolution swaps personality/goals deterministically.
