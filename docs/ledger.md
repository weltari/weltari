# ledger — apps/server/src/ledger + storage/repositories/ledger.ts

Purpose: the Job Ledger (Brief §2.2) — every piece of cold-path work is a durable row with an idempotency key, a lease, retries, and a dead-letter lane. Jobs are idempotent projections of the event log; startup *is* recovery.

## Contract

- Inputs: `enqueue()` calls (engine, scheduler); registered job handlers.
- Outputs: job state transitions; `job.failed` / `job.parked` events appended in the same WriteGate transaction as the row change.
- Single-writer authority: `storage/repositories/ledger.ts` is the only SQL site for `ledger_jobs`.
- Never: auto-retry a parked job; retry a `bug`-kind failure; put prompt content in `last_error` (C7/C12); do cron work inline (croner only writes rows).

## State machine

`pending` → (claim: attempts+1, lease) → `running` → `committed` | `failed` (operational, backoff `2^attempts`s cap 300, claimable again at `run_at`) | `parked` (dead-letter: max attempts exhausted, or any `bug`). `corrupt_state` touches no row and escalates to `onFatal` (crash-only, C5). Expired leases sweep back to `pending` on every poll and at startup — a kill -9 mid-job burns one attempt and gets retried.

Per-world serialization: rows carry `serial_group` (e.g. `world_agent:<world_id>`); the claim query skips a group that already has a running job (World Agent = 1 per world).

Lease-expiry overlap (week-8 hardening, the week-7 painter bug class — [painter.md](painter.md)): a slow LLM call can outlive its lease; the sweep reclaims the "dead" job and a second execution overlaps the first. Every handler with an `await` between its idempotency check and its commit append therefore re-checks the natural key **synchronously fused to the append** (no `await` between them — executions interleave only at await points in this single-process runtime): reflection, world-agent, world-cron (both classes), materialize, painter. The overlap costs one duplicate generation and a `warn`; never a duplicate event. Each handler has an interleaved-execution regression test (gated slow client, two executions of one job, exactly one event).

## File table

| File | What it does / talks to |
| --- | --- |
| `storage/repositories/ledger.ts` | Sole SQL site: `enqueue` (UNIQUE-key no-op), transactional `claimNext` (serial-group aware), `markCommitted/markRetry/markParked`, `sweepExpiredLeases`, `listActive` (scene-open blocking reads it). Rows validated on read; garbage is `CorruptStateError`. |
| `ledger/handlers/reflection.ts` | First real job handler: idempotent projection — if `reflection.committed` for (scene, character) exists it no-ops, so the post-kill lease retry is safe. FakeLLM-driven in tests/harness; LLM failure rethrows operational (runner retries, C7). Fault point `mid_reflection` right before the commit append. M6 part 2: the scene-origin CACHE line (`cache.appended`, Rev 4 §11 — the character's own summary as its "just happened to me" pointer) rides the SAME transaction via `sink.appendMany`, so a kill can never split reflection from its CACHE entry. M7 part 1 (Rev 4 §11, owner rulings 2026-07-11): the call now offers the `reflection` toolset and the SAME transaction also carries the gated memory outputs — 1–3 `memory.delta_committed` (cap enforced at gate 2), ≤1 `memory.core_updated` (full snapshot, last call wins), ≤1 `character.evolved` (refused whole for a `locked` character — zero rows, I8); all text sanitized for prefix hygiene (`engine/memory.ts gateReflectionMemory`). New fault point `mid_memory_commit` inside the memory-commit window; the job rides the character's memory mailbox (`serial_group memory:<world>:<char>`, set at the scene-end fan-out). |
| `ledger/handlers/world-agent.ts` | Same shape for the per-world World Agent pass (`serial_group` = `world_agent:<world_id>`, one running per world); commits `world_agent.committed` once per scene. M6 part 2 (Rev 4 §10): the pass also writes the SUBWIKI — one `subwiki.updated` per Narrator-created sublocation that participated in the scene (created in it, or moved into; owner rule: created = gets a wiki, transient/mentioned-only never), observable-now snapshots generated per stub; ALL entries + world_agent.committed land in ONE transaction, so the scene's single natural key keeps the whole pass exactly-once under kill-retry. Week 19 (§10 source-typing, hardened): the wiki calls read a NARRATION-ONLY transcript (`sceneNarrationTranscript` — `character` steps excluded by the committed step's own `call` label), so speech is wiki-ineligible by construction, not by prompt wording; the summary note still reads the whole scene. |
| `ledger/handlers/reflect-chat.ts` | The reflect_chat job (M6 part 2, Rev 4 §8): the chat analogue of reflection, enqueued atomically with `chat.ended` when a conversation range closes (exit / idle / startscene). Reads the range's messages, commits ONE `reflect_chat.committed` — idempotent per (conversation_id, range_end_id) with the fused lease-overlap re-check + interleaved-execution regression test; fault point `mid_reflect_chat` before the commit append; `verify-consistency` sweeps the natural key AND the chat-end atomicity (chat.ended ⇒ its job row, block 4h). No scene-style summary product (sessionsummarist = FALSE). M7 part 1: identical memory-output treatment as reflection.ts (toolset `reflection`, gated deltas/core/evolution with origin `chat` in ONE transaction via `sink.appendMany`, fault point `mid_memory_commit`, `serial_group memory:<world>:<char>` at all reflect_chat enqueue sites — DM exit/idle AND per-member group close). |
| `ledger/handlers/memory-compaction.ts` | The memory_compaction job (M7 part 1, Rev 4 §11): summarizes the character's deltas older than the window (`MEMORY_COMPACT_KEEP` = 10 newest stay raw; trigger at 16 uncompacted) into ONE cumulative `memory.compacted` covering every delta ≤ `up_to_id`. Deltas NEVER leave the log or the Search Index — the record is a reading convenience `archiveView` prefers. Repair for free: a `repair: true` re-run appends a SUPERSEDING record for the same range (latest wins in the fold) — no deletion exists or is needed (I1 intact). Idempotent per (character, up_to_id), fused re-check, fault point `mid_compaction`; enqueued by both reflection handlers post-commit + the main.ts boot sweep, on the character's memory mailbox. Kind `compaction` routes through the LLM seam; the fake scripts a deterministic summary at $0. |
| `ledger/handlers/cache-prune.ts` | The cache_prune job (M7 part 1, Rev 4 §11 retention: keep the last N = `WELTARI_CACHE_KEEP`, default 50): pure code, no LLM. Appends the `cache.pruned` WATERMARK — every CACHE view ignores entries at or below it (a lane whose only entry sank below the watermark disappears: that is what pruning means); replay rebuilds the identical pruned view, nothing is deleted (I1). Idempotent by recomputation (the due-check against the CURRENT log is the gate); safe by construction — reflection reads session history, never CACHE history. Enqueued like compaction (reflection post-commit + boot sweep), same memory mailbox. |
| `ledger/handlers/profile-analysis.ts` | The profile_analysis job (M7 part 2, Rev 4 §9 Job 2): the GM's analysis skill over an ENDED scene or chat range — kind `profile_analysis` on the GM's route (char:gm), GM-profile context, output = JSON `{hypotheses: [1–5 one-liners]}` through `parseLlmJson` + `validateAt` (B6 gate 1), then B14 hygiene sanitization (gate 2) — the instruction optimizes for STORY-QUALITY signals, never raw time-spent. Writes side-store rows + `profile.updated` (a COUNT — never the text) in ONE transaction. Consent-gated twice: the enqueue sites (scene-lifecycle fan-out for `user:`-actor ends; the chat endRange, whose subject is the conversation's OWNER parsed from the stable id — an idle sweep's system actor never mis-profiles) fire only while the `profiling_enabled` fold is on, and the handler re-checks at run — a toggle-off between enqueue and run means ZERO writes. Idempotent per (actor, context) via the store's own hasContext (eager + fused re-check); natural key `profile_analysis:<actor>:<context>`; fault point `mid_profile_analysis`; serializes per world (`serial_group profile:<world>` — the store's sole writer, Rev 4 §4.3). |
| `ledger/handlers/object-gc.ts` | The object_gc job (M7 part 3, Rev 4 §7): the GC sweep — pure code, no LLM. Payload-less, sublocation-held objects never touched outside their ENDED creating scene are tombstoned with `object.swept` (actor `system:object_gc`); the row leaves the projection in the SAME transaction while the log stays append-only (I1: the tombstone IS the deletion). Payload carriers are exempt by the candidate rule; proposal-applied objects (no creating scene) are never candidates. Candidates recomputed INSIDE the transaction — a concurrent touch can never lose the race, and a retried sweep converges to zero duplicate tombstones. Enqueued by the scene-end fan-out (natural key `object_gc:<world>:<scene>`); fault point `mid_object_gc`; serializes per world (`serial_group object_gc:<world>`). |
| `ledger/handlers/proactive-dm.ts` | The proactive_dm job (M6 parts 3–4, Rev 4 §8 — proactive CRON DMs; owner rulings 2026-07-10/11): a GAME-time occurrence fire (enqueued only when the world clock advances — a paused world sends nothing) picks ONE character via up to 5 SALTED deterministic FNV picks over ALL profiles (never a pre-filtered pool, so the few available characters are not forced to carry every fire; all 5 ineligible → the occurrence stays quiet; "random in V1" that survives kill-retries), EAGERLY generates its DM (chat-shaped context incl. the conduct skill; the character may DECLINE via the explicit stay_silent tool — neutral prompt, its own choice, nothing durable; the instruction forbids startscene — CRON never opens scenes in V1, stray calls are ignored un-honored) and commits message + CACHE + `chat.outreach_recorded` in ONE transaction — the push IS the message. Eligibility is a pure fold ([engine.md](engine.md) outreach.ts): presence available ∧ (answered thread → QUIET, no open range ∣ unanswered → growing backoff base ×2^n) ∧ not frozen. The third unanswered outreach appends `chat.thread_frozen` in the SAME transaction (the durable gateway hook; a user reply thaws by construction). Outreach entries stamp BOTH clocks (occurrence_iso = the game-time boundary since 0.13.0, game_time = the clock at fire); backoff and the freeze run on the game axis. Idempotent per (world, occurrence_iso) with the fused lease-overlap re-check + interleaved-execution test; fault point `mid_proactive_dm`; fires serialize per world (`serial_group proactive_dm:<world>` — two due occurrences must SEE each other's outreach, never double-send); verify-consistency sweeps the natural key, the freeze key, the outreach⇒message atomicity and the sub-cap-freeze ban (block 4i). |
| `ledger/handlers/social-post.ts` | The social_post job (M6 part 5, Rev 4 §12 — the Feed; owner rulings 2026-07-11): a GAME-day cadence fire (default 2/game day via `WELTARI_SOCIAL_POSTS_PER_DAY`; enqueued only when the world clock advances, newest ≤10 boundaries per skip — `SOCIAL_POST_SKIP_CAP`, the freshest window survives) picks ONE poster via up to 5 salted FNV picks over ALL profiles (eligible = not in a scene; a character with no acquaintances still posts — the user reads every post, delivery is what acquaintance gates), EAGERLY generates the post (kind `social_post`, chat toolset — grounded in the poster's CACHE recap/goals like a proactive DM; stay_silent declines, startscene is prompted away and ignored) and commits `social.post_committed` + the poster's `cache.appended` (origin `social`) + one `social_reaction` decision job per PICKED recipient in ONE transaction (`sink.appendManyWithJobs` — the scene-end fan-out shape: a kill between post and reactions cannot exist). Recipients = the acquaintance fold ([engine.md](engine.md) social.ts); the reaction pick is `pickReactionCandidates` (salted hash ranking, cap `WELTARI_SOCIAL_REACTION_CAP` default 4). Idempotent per (world, occurrence_iso) with the fused lease-overlap re-check; fault point `mid_social_post`; fires serialize per world (`serial_group social_post:<world>` — scheduled-game-timestamp order holds). |
| `ledger/handlers/social-reaction.ts` | The social_reaction job (M6 part 5, Rev 4 §12): ONE skill-triggered decision for one picked recipient — the post rides the dynamic tail as an external line (B14); toolset `social_react` (react like/one-line-comment + stay_silent + cache, all data-only). Gate 2 enforces body-iff-comment (a like's stray body is dropped; a comment without one is no reaction). A reaction commits `social.reaction_committed` (reaction_id = `<post>:<character>`) + the reactor's `cache.appended` (origin `social`) atomically — the two-sided memory write's reactor half; stay_silent / no valid react = nothing durable. Comments are isolated in V1 (characters never react to comments). Natural key (post_id, character_id) + fused re-check; runs on the character's own serial group (`social:<world>:<character>` — the mailbox rule); a missing post is corrupt_state (the job is enqueued atomically with its post). |
| `ledger/handlers/social-reply.ts` | The social_reply job (M6 part 5, owner ruling 2026-07-11): the comment author's ANSWER-ONLY reply to the user's feed reply — toolset `social_reply` carries nothing but cache, so the character physically cannot promise meetings/actions from the thread (and the conduct skill says so). Characters always answer (availability is the product); an empty generation is OPERATIONAL (retry — the user is waiting), never a silent skip. The whole thread (post → comment → every reply exchange) rides the prompt; commits `social.reply_answered` + the answerer's `cache.appended` (origin `social`) atomically. Natural key `in_reply_to` (one answer per user reply) + fused re-check; runs on the author's social serial group; a missing post/comment is corrupt_state. |
| `ledger/runner.ts` | The ONE catch site for job execution (C7): exhaustive `switch` on error kind → retry / park / fatal; emits job events atomically with the row change. `tick()` is pull-based so tests never sleep. |
| `ledger/scheduler.ts` | croner wrapper: computes next occurrence (UTC), writes a future-dated ledger row keyed `cron:<type>:<world>:<occurrence>` — idempotent across restarts. Also the pure fictional-calendar helpers the engine's WorldClock uses (`addMinutesIso`, `occurrencesBetween` — croner never reads the wall clock there). M6 part 4: `intervalOccurrencesBetween` — epoch-aligned GAME-time interval boundaries in (from, to] for the proactive-DM cadence (croner can't express fractional-minute cadences; main.ts's advance-time wrapper enqueues the newest ≤3 boundaries the skip crossed as idempotent rows, key `proactive_dm:<world>:<occurrence>` — a paused world enqueues nothing, owner ruling 2026-07-10/11). M6 part 5: the Feed rides the same helper — cadence 1440/`WELTARI_SOCIAL_POSTS_PER_DAY` minutes, newest ≤10 boundaries per skip, key `social_post:<world>:<occurrence>`. |
| `ledger/handlers/world-cron.ts` | Time-skip replay handlers (`world_cron.code` / `world_cron.llm`): idempotent per (cron_type, scheduled_for) via the committed event; code = pure projection, llm = FakeLLM/real narration; fault point `mid_cron` before the commit append. M7 part 4: the code handler gains the kind-routed `occurrenceEvents` planner hook — main routes `world_movement` → `planMovementEvents` and `encounter_marker` → `planCronMarkerDrop` ([engine.md](engine.md)); the planned domain events append atomically WITH the completed event in one `sink.appendMany`, so the completed natural key gates the whole batch and a kill leaves either the whole occurrence or none of it. |
| `ledger/handlers/painter.ts` | Painter job handler — documented in [painter.md](painter.md). |
| `ledger/handlers/materialize.ts` | The materialize job (M4 part 2, Rev 4 §14): LLM invents ONLY the stub (name + description) — placement is code-owned (the square is in the payload, from the user's Explore click). Full B6 double gate: `validateAt('llm', 'materialize.stub', …)` over the parsed JSON (via `llm/structured.ts`), then engine-state gate (square still empty, world exists); the only durable write is one `sublocation.materialized`. Idempotent per square (deterministic `subloc:sq-<col>-<row>` id + occupancy check) — the post-kill lease retry converges. Fault point `mid_materialize` before the commit append. M6 part 1: a second payload shape `{stub_sublocation_id, anchor}` materializes a Narrator stub — ZERO LLM calls (the identity already exists; a missing stub event is corrupt_state): `solveFrontierSquare` picks the square at execution time, the event keeps the stub's id/name/description, idempotent per stub id with the fused lease-overlap re-check; a full map is a warn + no-op (the stub stays map-less), a square claimed inside the fault window is an operational retry (the retry re-solves). |
| `ledger/handlers/map-edit.ts` | The map_edit job (M5 part 2, Rev 4 §14 Flow A): the GM form — LLM invents ONLY name + description from the user's intent (delimiter-wrapped external text, B14); geometry (bounding box, mask, centroid) is code-owned via `editGeometry`. Full B6 double gate (schema gate on the form, engine-state gate: world exists, centroid on explored ground); the only durable write is one `sublocation.created` (actor = the requesting user; pin at the mask centroid, footprint = the drawn polygon), then the edit's painter job enqueues (key `painter:map:<world>:edit-<edit_id>`, deduped — the created-exists path re-enqueues to heal a kill in between). Fault point `mid_map_edit` + the fused lease-overlap re-check. |
| `ledger/handlers/map-click.ts` | The map_click job (M5 part 2, Rev 4 §14 Flow B steps 2–5): crops the CURRENT composite around the click (painter-owned `clickWindow`/`cropRegionPng`), the VLM classifies it with nearby DB labels as anchors (`classify_click`), the story LLM invents INSIDE the classification (`jump_in` kind), the invention's creation flag decides persist-or-discard. Two model outputs, each through the full B6 double gate (garbage classification/invention → rejected, zero rows, retry regenerates). The ONLY durable write is one `map_click.resolved`: outcome `created` IS the persistent spawn's sublocation row (registry-projected, pin at the click point); `transient` never becomes a sublocation. Fault point `mid_map_click` + the fused lease-overlap re-check. |
| `ledger/handlers/update-check.ts` | update_check job (startup + croner, FINAL item 12): fetch the release channel, `validateAt('update', …)`, announce a strictly-newer version as ONE `update.available` event (idempotent). Never downloads — see [update.md](update.md). Week 19 (audit item 5): a 404 = "no releases published yet" (GitHub's answer while a repo has zero releases — the standing dev state) completes cleanly with zero events instead of parking a retrying failure every boot; every other non-ok status stays a typed retryable error. |
| `ledger/handlers/update-apply.ts` | update_apply job (serial_group `update_apply`): re-fetch release, download artifact trio, verify SHA-256 + minisign, stage + pointer flip (B12), append `update.staged` once — kill -9 retries converge. Fault point `mid_update` inside stageUpdate. |
| `../migrations/0002_jobs.sql` | `ledger_jobs` table: states CHECK, idempotency UNIQUE, lease columns, `serial_group`, claim indexes. |

## Events consumed/emitted

Emits `job.failed`, `job.parked` (actor `system:ledger`); handlers emit `reflection.committed` (actor = the character), `world_agent.committed` (actor `system:world_agent`) and `sublocation.materialized` (actor `system:engine`). Consumes `turn.committed` (scene transcripts) and reads its own committed events for idempotency.

## Configuration

`WELTARI_LEASE_SECONDS` (default 60) — job lease length; the kill harness shortens it so a killed-mid-job lease expires within one cycle. `WELTARI_CRON_DM_GAME_MINUTES` (default 1440 = once per fictional day, ON by default — owner ruling 2026-07-11; 0 = disabled) — the proactive-DM cadence in GAME minutes: occurrences enqueue only when the world clock advances (owner ruling 2026-07-10/11 — never on wall time), capped at the newest 3 per advance (the freeze cap makes more pure spend). `WELTARI_MARKER_MIN` / `WELTARI_MARKER_MAX` (defaults 1 / 5, Rev 4 §15) — the chance-encounter marker floor/ceiling the engine enforces structurally; `WELTARI_MARKER_TTL_GAME_MINUTES` (default 180) — the default GAME-time TTL stamped at drop (M7 part 4, [engine.md](engine.md) markers.ts).

## Tests

- Invariants (I3): `ledger-idempotency`, `ledger-lease-expiry`, `ledger-dead-letter`, `ledger-per-world` — all on `FakeClock`, zero sleeps.
- Unit: runner C7 mapping (one error of each kind through stub jobs, plus untyped throw and missing handler), scheduler occurrence idempotency.
